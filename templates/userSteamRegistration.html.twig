{% extends 'InsaLanUserBundle::layout.html.twig' %}

{% block body %}
{{ parent() }}

<div class="frame frame-active">
  <header class="full">
    <div class="step pull-left">
      &#x2699;
    </div>
    <h2 class="title pull-left">Mon profil - Connexion Steam</h2>
    <br class="clear">
  </header>

  <div class="text" style="text-align: center;">
    <br/>
    {% if connectedAccount != null %}
      <div>
        Vous êtes connecté en tant que : <strong>{{connectedAccount}}</strong>
        <img src="{{avatarSteamSrc}}"/>
      </div>
      <br/><br/>
      <a href="{{ path ('app_user_index') }}" class="btn btn-primary ctrl grid-4 pull-left">Retour</a>
      <div>
        <a class="btn btn-danger ctrl grid-6 pull-left" href="{{deleteLink}}">Supprimer le lien avec ce compte steam</a>
      </div>
    {% else %}
      En cliquant sur le bouton ci-dessous, vous serez redirigé vers le site steam afin de vous connecter.<br />
      Nous pourrons ainsi vérifier votre identifiant pour les tournois nécessitant Steam
      <br/><br/>
      <a href="{{url}}">
        <img src="https://steamcommunity-a.akamaihd.net/public/images/signinthroughsteam/sits_01.png" alt="Se connecter">
      </a>
      <br/><br/>
      <a href="{{ path ('app_user_index') }}" class="btn btn-danger ctrl grid-2 pull-left">Retour</a>
    {% endif %}
  </div>
</div>
{% endblock %}
