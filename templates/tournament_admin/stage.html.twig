{% extends 'Admin/adminLayout.html.twig' %}

{% block body %}
{{ parent() }}

<article>
  <header>
    <h2>{{ stage.name }} - {{ stage.tournament.name }}</h2>
    <a href="{{ path("app_tournamentadmin_index_1", {id: stage.tournament.id}) }}">
      <<< Retour
    </a>
  </header>

  {% include 'Part/AdminMatches.html.twig' %}
</article>
{% endblock %}
