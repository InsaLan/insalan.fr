{% extends 'InsaLanArchivesBundle::layout.html.twig' %}

{% block stylesheets %}
{{ parent() }}
{% stylesheets 'bundles/insalan/css/archives.css' %}
<link rel="stylesheet" href="{{ asset_url }}" />
{% endstylesheets %}
{% stylesheets 'bundles/insalan/css/tournament.css' filter='cssrewrite' %}
<link rel="stylesheet" href="{{ asset_url }}" />
{% endstylesheets %}
{% endblock %}

{% block body %}
{{ parent() }}

<article>
  <header>
    <h2>Année {{year}}</h2>
  </header>

  {% if poster is defined %}
  <section>
    <div class="previous-years-list">
      <div class="previous-years">
        <img src="{{ asset('bundles/insalan/img/')  ~ poster}}" alt="InsaLan {{year}}" />
      </div>
    </div>
  </section>
  {% endif %}

  {% if trailer is defined %}
  <section>
    <header>
      <h2>Trailer</h2>
    </header>
    <div>
      <iframe width="700" height="437" src="{{ trailer }}" frameborder="0" allowfullscreen></iframe>
    </div>
  </section>
  {% endif %}

  {% if aftermovie is defined %}
  <section>
    <header>
      <h2>Aftermovie</h2>
    </header>
    <div>
      <iframe width="700" height="437" src="{{ aftermovie }}" frameborder="0" allowfullscreen></iframe>
    </div>
  </section>
  {% endif %}

  {% if old_tournaments is not empty %}
  <section>
    <header>
      <h2>Tournois</h2>
    </header>
    {% include 'InsaLanTournamentBundle:Part:TournamentList.html.twig' with {'tournaments': old_tournaments} %}
  </section>
  {% endif %}

  {% if picturesAlbum is not empty %}
  <section>
    <header>
      <h2>Photos</h2>
    </header>
    <div class="album-list ">
      {% for a in picturesAlbum %}
      {% if a.album != ""%}
      <a class="btn2 btn2-nocolor" href="{{ path('archives_pictures', {'year': year, 'album': a.album}) }}">{{ a.album }}</a>
      {% endif %}
      {% endfor %}
    </div>

  </section>

    {% endif %}

    {% if streamsAlbum is not empty %}
    <section>
      <header>
        <h2>Streams</h2>
      </header>
      <div class="album-list ">
        {% for a in streamsAlbum %}
        {% if a.album != ""%}
        <a class="btn2 btn2-nocolor" href="{{ path('archives_streams', {'year': year, 'album': a.album}) }}">{{ a.album }}</a>
        {% endif %}
        {% endfor %}
      </div>

    </section>
    {% endif %}

  </article>
{% endblock %}
