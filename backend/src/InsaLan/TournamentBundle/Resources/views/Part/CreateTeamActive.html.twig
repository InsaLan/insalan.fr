    <div class="frame frame-active">
      <header class="full">
        <div class="step pull-left">
            {{ counter }}
        </div>
        <div class="title pull-left">Créer une équipe</div>
        <br class="clear">
      </header>

          {{ form_start(form) }}

          {{ form_widget(form._token) }}
          {{ form_errors(form) }}

          <div class="input field">
            {{ form_widget(form.name, {'attr': {'placeholder': 'Nom de votre équipe'}}) }}
          </div>


          <div class="input field">
            {{ form_widget(form.plainPassword.first, {'attr': {'placeholder': 'Mot de passe'}}) }}
          </div>

          <div class="input field">
            {{ form_widget(form.plainPassword.second, {'attr': {'placeholder': 'Confirmation de mot de passe'}}) }}
          </div>

          <a class="btn btn-danger ctrl grid-2 pull-left" href="{{ path('insalan_tournament_user_jointeam', {tournament: registrable.id}) }}">Annuler</a>
          <a class="btn btn-primary ctrl grid-8 pull-left" href="javascript:{}" onclick="document.getElementById('step4').submit();">Créer mon équipe</a>
          <br class="clear">
          {{ form_end(form) }}
    </div>
